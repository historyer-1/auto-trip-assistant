任务描述：
1.在AgentTripController中编写新请求，查询当前用户的所有旅行计划。你写一个合适的请求路径，然后和其他请求一样逻辑在service中完成，返回service返回的结果。数据库操作在dao层。
2.给数据库的旅行计划表的用户id字段添加索引，以增加任务1的查询速度。
3.前端将交给团队中的其他伙伴完成，请你根据后端代码内容用markdown格式写好api文档，每一条请求包含请求路径，请求需要携带的信息等，内容清晰而不臃肿。
4.帮我在根目录编写一份readme.md，内容包含项目介绍，如果要部署的运行环境搭建（如kafka和redis的部署和端口设置），项目的启动步骤。要保证第三方开发者能够根据readme重新部署我的项目。


技术细节要求：
1.任务1的参数中要包含分页选项，可以控制每次返回最多n条数据，使前端能够按页加载。每次返回一个TripPlan的列表。
2.api文档不需要太花哨的内容，清晰体现前端开发所需的必要信息即可

代码完成后要求：
1.做一次代码检查，保证项目能够正常工作


全局要求：
1.先进行Plan再执行
2.修改的代码附上合适的中文注释，包括函数参数，返回值和功能，函数内的代码块写上逻辑和功能。
3.代码逻辑尽可能简单高效（重要！），除非特别必要，否则不要为了兜底添加复杂的解析格式函数，数转换格式函数，辅助函数。



