任务描述：
1.帮我修改提示词，约束行程规划输出格式 
2.新建一个独立文件一个格式化类，里面定义格式化方法，把这个方法导入到行程规划智能体类进行兜底，如果格式不合预期，就使用格式化函数

技术细节要求：
1.任务中要求创建的每个类都创建在独立文件中

代码完成后要求：
1.做一次代码检查，保证项目能够正常工作

全局要求：
1.先进行Plan再执行.
2.修改的代码附上合适的中文注释，包括函数参数，返回值和功能，函数内的代码块写上逻辑和功能。
3.代码逻辑尽可能简单高效（重要！），除非特别必要，否则不要为了兜底添加复杂的解析格式函数，数转换格式函数，辅助函数和臃肿的标准化壳。如果一定要添加，添加在trip_plan_formatter.py中。




uvicorn FastapiPan:app --reload --port 8000