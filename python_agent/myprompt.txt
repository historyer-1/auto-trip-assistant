任务描述：
1.在AmapTool.py增加天气搜索工具，使用高德api提供的功能，详细信息在技术细节提供。
2.参照attraction_search_agent.py，修改剩下三个搜索智能体，三个都是调用AmapTool.py中的工具，其中只有天气搜索智能体调用搜索工具。行程规划和编排不需要修改。
3.在test.py参照景点搜索测试函数编写其他三个搜索智能体的测试函数，每个智能体编写一个单独的测试函数。
4.参照景点搜索模型的提示词修改其他三个智能体的提示词

技术细节要求：
1.高德天气查询url：https://restapi.amap.com/v3/weather/weatherInfo?parameters，请求方式：Get,参数为{
   1. key 请求服务权限标识
   2. city 城市编码 输入城市的 adcode，adcode 信息可参考AMap_adcode_citycode.xlsx
}。
2.如果AMap_adcode_citycode.xlsx大模型不能直接读取，在大模型调用天气工具时把信息传给他并完善相关逻辑。

代码完成后要求：
1.做一次代码检查，保证项目能够正常工作

全局要求：
1.先进行Plan再执行.
2.修改的代码附上合适的中文注释，包括函数参数，返回值和功能，函数内的代码块写上逻辑和功能。
3.代码逻辑尽可能简单高效（重要！），除非特别必要，否则不要为了兜底添加复杂的解析格式函数，数转换格式函数，辅助函数和臃肿的标准化壳。如果一定要添加，添加在trip_plan_formatter.py中，并删减里面已经无效的兜底函数。




uvicorn FastapiPan:app --reload --port 8000