"""
本文件用于管理本地 API Key。

为什么单独放一个文件？
1. 便于把敏感信息集中管理；
2. 便于加入 .gitignore，避免提交到远程仓库；
3. 业务代码只依赖这里暴露的变量，不直接写死密钥。

注意：
- 这个文件是“本地私有配置”，不要提交到 Git；
- 你可以直接把字符串改成自己的 key，或者改成从环境变量读取。
"""

# 高德 Web 服务 Key（请替换成你自己的真实 key）
# 示例格式通常是 32 位字符串。
AMAP_WEB_API_KEY = "0c96342556d15ebda3bb72a144ce81c8"

# 千问api
QWEN_API_KEY = "sk-285342b1a39d495abdeacac644a5c320"

# 统一模型配置，后续只需要修改这一处。
MODEL = "qwen3.6-plus-2026-04-02"
